{% extends "P5CoreBundle::layout.html.twig" %}

{% block body %}
	<div class="col-sm-8">
		<h3 id="forgot">Mot de passe oublié ?</h3>
		<hr id="separate">
		{{form(form)}}
		{% for message in app.flashes('notice') %}
			<div class="alert alert-success" id="flashmail">
       	 		{{ message }}
    		</div>
		{% endfor %}		
	</div>
{% endblock %}