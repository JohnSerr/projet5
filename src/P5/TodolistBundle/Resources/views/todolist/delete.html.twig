{% extends 'P5TodolistBundle::layout.html.twig' %}

{% block p5todolist_body %}

<h3>Supprimer un rappel</h3>

<div>
	<table class="table table-striped">
		<tr>
			<th class="col-md-3">Titre</th>
			<th class="col-md-3" id="hidedate">Ajouté le</th>
			<th class="col-md-3">To Do</th>
			<th class="col-md-3">Date limite</th>
		</tr>
		<tr>
			<td class="hidetitle wrap">{{todo.title}}</td>
			<td class="adddate wrap">{{todo.date|date("d-m-Y")}}</td>
			<td class="wrap" id="content">{{todo.content}}</td>
			<td class="wrap">{{todo.dateofend|date("d-m-Y")}}</td>
		</tr>	

	</table>
</div>
	
	<hr id="separate" />

	<div id="warning_delete">
		<p>Êtes-vous certain de vouloir supprimer ce rappel ?<p>
	</div>

<form action="{{path('p5_todolist_delete', {'id' : todo.id }) }}" method="post">
	<a href="{{path('p5_todolist_view')}}" role="button" class="btn btn-info"><i class="fas fa-angle-left"></i> Revenir à la liste</a>
	<input type="submit" name="supprimer" value="Supprimer" class="btn btn-danger">
</form>

{% endblock %}